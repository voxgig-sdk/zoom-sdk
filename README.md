# Zoom SDK

Zoom Meetings API client, generated from the OpenAPI spec.

Create, read, update, and delete Zoom meetings.

Learn more about Zoom Meetings API at [zoom.us](https://zoom.us).

This is an unofficial SDK for the Zoom Meetings public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

> TypeScript, Python, PHP, Golang, Lua, JavaScript SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

## Entities, not endpoints

This SDK exposes the API as a small set of **semantic entities** — Meeting — that you
call directly, instead of assembling URL paths and query strings. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `load`, `create`, `update`, `remove`):

```ts
const client = new ZoomSDK()
const items = await client.Meeting().list({ user_id: "example" })
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = ZoomSDK.test({
  entity: {
    meeting: {
      test01: { id: 'test01', user_id: 'example_user_id', topic: 'example_topic' },
    },
  },
})
const meetings = await client.Meeting().list()
// meetings is an array of Meeting entities, populated with mock data
// — call meetings[0].data() for the record itself
console.log(meetings)
```

### Python

```python
client = ZoomSDK.test()
meetings = client.Meeting().list()
print(meetings)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = ZoomSDK::test([
    "entity" => ["meeting" => ["test01" => ["id" => "test01"]]],
]);
$meetings = $client->Meeting()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.Meeting(nil).List(
    nil, nil,
)
```

### Lua

```lua
local client = sdk.test()
local results, err = client:Meeting():list()
```

### JavaScript

```js
const client = ZoomSDK.test()
const meetings = await client.Meeting().list()
// meetings is an array of entities, populated with mock data
// — call meetings[0].data() for the record itself
console.log(meetings)
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/zoom` | publish pending — [install from git tag](https://github.com/voxgig-sdk/zoom-sdk/releases) |
| Python | `voxgig-sdk-zoom` | publish pending — [install from git tag](https://github.com/voxgig-sdk/zoom-sdk/releases) |
| PHP | `voxgig-sdk/zoom` | publish pending — [install from git tag](https://github.com/voxgig-sdk/zoom-sdk/releases) |
| Golang | `github.com/voxgig-sdk/zoom-sdk/go` | `go get github.com/voxgig-sdk/zoom-sdk/go@latest` |
| Lua | `voxgig-sdk-zoom` | publish pending — [install from git tag](https://github.com/voxgig-sdk/zoom-sdk/releases) |
| JavaScript | `@voxgig-sdk/zoom-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/zoom-sdk/releases) |
| Go CLI | `github.com/voxgig-sdk/zoom-sdk/go-cli` | `go install github.com/voxgig-sdk/zoom-sdk/go-cli/cmd/zoom@latest` |
| Go MCP server | `github.com/voxgig-sdk/zoom-sdk/go-mcp` | `go get github.com/voxgig-sdk/zoom-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { ZoomSDK } from '@voxgig-sdk/zoom'

const client = new ZoomSDK({
  apikey: process.env.ZOOM_APIKEY,
})

// List all meetings (returns MeetingEntity[] — .data() for the record)
const meetings = await client.Meeting().list({ user_id: "example" })
for (const meeting of meetings) {
  console.log(meeting)
}
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Lua, JavaScript) | `ts/` `py/` `php/` `go/` `lua/` `js/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o zoom-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "zoom": {
      "command": "/abs/path/to/zoom-mcp"
    }
  }
}
```

## Entities

The API exposes one entity:

| Entity | Description | API path |
| --- | --- | --- |
| **Meeting** | The Meeting entity (create, list, load, remove, update). | `/users/{userId}/meetings` |

The operations available across these entities are **load**, **list**, **create**, **update**, **remove** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from zoom_sdk import ZoomSDK

client = ZoomSDK({
    "apikey": os.environ.get("ZOOM_APIKEY"),
})

# List all meetings (returns a list, raises on error)
meetings = client.Meeting().list({"user_id": "example"})
for meeting in meetings:
    print(meeting)

# Load a specific meeting (returns the record, raises on error)
meeting = client.Meeting().load({"id": 1})
print(meeting)
```

### PHP

```php
<?php
require_once 'zoom_sdk.php';

$client = new ZoomSDK([
    "apikey" => getenv("ZOOM_APIKEY"),
]);

// List all meetings (returns an array; throws on error)
$meetings = $client->Meeting()->list();
print_r($meetings);

// Load a specific meeting (returns the ENTITY; call data_get() for the record; throws on error)
$meeting = $client->Meeting()->load(["id" => 1]);
print_r($meeting);
```

### Golang

```go
import sdk "github.com/voxgig-sdk/zoom-sdk/go"

client := sdk.NewZoomSDK(map[string]any{
    "apikey": os.Getenv("ZOOM_APIKEY"),
})

// List all meetings
meetings, err := client.Meeting(nil).List(nil, nil)
if err != nil {
    panic(err)
}
fmt.Println(meetings)
```

### Lua

```lua
local sdk = require("zoom_sdk")

local client = sdk.new({
  apikey = os.getenv("ZOOM_APIKEY"),
})

-- List all meetings
local meetings, err = client:Meeting():list()
print(meetings)

-- Load a specific meeting
local meeting, err = client:Meeting():load({ id = 1 })
print(meeting)
```

### JavaScript

```js
const { ZoomSDK } = require('@voxgig-sdk/zoom-js')

const client = new ZoomSDK({
  apikey: process.env.ZOOM_APIKEY,
})

// List all meetings (returns an array)
const meetings = await client.Meeting().list({ user_id: "example" })
for (const meeting of meetings) {
  console.log(meeting)
}
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **TestFeature** | In-memory mock transport for testing without a live server |

Pass custom features via the `extend` option at construction time.

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Lua](lua/README.md)
- [JavaScript](js/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: [https://api.zoom.us/v2](https://api.zoom.us/v2)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the Zoom Meetings API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
