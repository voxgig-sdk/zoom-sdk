<?php
declare(strict_types=1);

// Zoom SDK utility: feature_hook

class ZoomFeatureHook
{
    public static function call(ZoomContext $ctx, string $name): void
    {
        if (!$ctx->client) {
            return;
        }
        $features = $ctx->client->features ?? null;
        if (!$features) {
            return;
        }
        foreach ($features as $f) {
            if (method_exists($f, $name)) {
                $f->$name($ctx);
            }
        }
    }
}
